defaults:
  provider: zhipu
  model: GLM-4-Flash
  temperature: 0.3
  max_retries: 3
  request_timeout: 60.0

models:
  chat:
    model: GLM-4-Flash
  plan:
    model: GLM-4-Flash
  summary:
    model: GLM-4-Flash